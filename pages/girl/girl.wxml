<!--pages/girl/girl.wxml-->
<view class="parent">
  <view class="list"  wx:for="{{array}}">
    <image class="nameTag" mode="aspectFill" src="{{item.name}}" background-size="cover"></image>
  </view>
</view>
